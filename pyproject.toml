[project]
name = "locky"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "argon2-cffi>=25.1.0",
    "cryptography>=48.0.0",
    "flask>=3.1.3",
]

[tool.uv.workspace]
members = [
    "web",
    "locks",
    "Locky",
]
